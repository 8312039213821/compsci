// state variables
// Sami
// Sep 25, 2024

let myColor;
let circleSize = 50;
let currentColor;


function setup() {
  createCanvas(windowWidth, windowHeight);
  myColor = color(100, 0, 200);
}

function draw() {
  background(myColor);
}
