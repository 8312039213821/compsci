// Tic Tac Toe challenge
// Sami
// Nov 6, 2024

let grid = [[0, 0, 0,],
            [0, 0, 0,],
            [0, 0, 0,]];


function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(220);
}
